include go.mk

# ------------------------------------------------------------------------------
# Global configuraiton.
# ------------------------------------------------------------------------------
REPO      = lunar
LIBS      = github.com/xiejw/${REPO}/...

# ------------------------------------------------------------------------------
# Actions.
# ------------------------------------------------------------------------------
#
.PHONY: compile ${LIBS}

compile: ${BUILD_DIR} ${LIBS} ${CMDB}

${LIBS}:
	${GO} $@

fmt:
	${FM} ${LIBS}

test:
	${TS} ${LIBS}

clean:
	${CL} go mod tidy

